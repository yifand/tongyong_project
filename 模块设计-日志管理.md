# PDI智能监测平台 - 日志管理模块详细设计文档

**版本**: v1.0
**日期**: 2026-03-29
**负责人**: 后端程序员-G
**模块**: pdi-log-management

---

## 目录

1. [模块职责简述](#1-模块职责简述)
2. [详细类设计](#2-详细类设计)
3. [数据库表设计](#3-数据库表设计)
4. [接口详细设计](#4-接口详细设计)
5. [AOP日志记录设计](#5-aop日志记录设计)
6. [日志清理策略](#6-日志清理策略)
7. [单元测试用例设计](#7-单元测试用例设计)

---

## 1. 模块职责简述

日志管理模块（pdi-log-management）是PDI智能监测平台的核心支撑模块，负责系统操作行为的审计追踪和系统运行状态的记录。主要包含以下职责：

### 1.1 核心职责

| 功能模块 | 职责描述 |
|----------|----------|
| **操作日志** | 记录用户的所有操作行为，包括登录、登出、配置修改、用户管理、数据导出等，支持多维度查询 |
| **系统日志** | 记录服务启动/停止、与盒子通信状态、异常错误等系统级事件，按级别分类管理 |
| **报警日志归档** | 与预警模块数据互通，独立存储便于长期保留 |
| **日志清理** | 定时清理过期日志，控制存储成本 |

### 1.2 模块依赖

```
pdi-log-management
    ├── depends on: pdi-common (公共基础模块)
    ├── used by: pdi-auth-management (权限管理模块，登录登出记录)
    ├── used by: pdi-system-config (系统配置模块，配置变更记录)
    └── used by: pdi-alarm-center (预警中心模块，报警日志归档)
```

---

## 2. 详细类设计

### 2.1 包结构

```
com.vdc.pdi.logmanagement/
├── controller/
│   ├── OperationLogController.java      # 操作日志查询API
│   └── SystemLogController.java         # 系统日志查询API
├── service/
│   ├── OperationLogService.java         # 操作日志服务接口
│   ├── OperationLogServiceImpl.java     # 操作日志服务实现
│   ├── SystemLogService.java            # 系统日志服务接口
│   ├── SystemLogServiceImpl.java        # 系统日志服务实现
│   └── LogCleanupService.java           # 日志清理定时任务
├── domain/
│   ├── entity/
│   │   ├── OperationLog.java            # 操作日志实体
│   │   └── SystemLog.java               # 系统日志实体
│   ├── repository/
│   │   ├── OperationLogRepository.java  # 操作日志Repository
│   │   └── SystemLogRepository.java     # 系统日志Repository
│   └── vo/
│       ├── OperationLogVO.java          # 操作日志值对象
│       └── SystemLogVO.java             # 系统日志值对象
├── dto/
│   ├── request/
│   │   ├── OperationLogRequest.java     # 操作日志查询请求
│   │   └── SystemLogRequest.java        # 系统日志查询请求
│   └── response/
│       ├── OperationLogResponse.java    # 操作日志查询响应
│       ├── SystemLogResponse.java       # 系统日志查询响应
│       └── PageResult.java              # 分页结果包装
├── aspect/
│   └── OperationLogAspect.java          # AOP切面记录操作日志
├── annotation/
│   └── OperationLog.java                # 操作日志注解
├── config/
│   └── AsyncConfig.java                 # 异步配置
├── enums/
│   ├── OperationType.java               # 操作类型枚举
│   └── LogLevel.java                    # 日志级别枚举
└── mapper/
    └── LogMapper.java                   # MapStruct映射器
```

### 2.2 实体类设计

#### 2.2.1 OperationLog（操作日志实体）

```java
@Entity
@Table(name = "sys_operation_log", indexes = {
    @Index(name = "idx_op_log_user_id", columnList = "user_id"),
    @Index(name = "idx_op_log_type", columnList = "operation_type"),
    @Index(name = "idx_op_log_result", columnList = "result"),
    @Index(name = "idx_op_log_created", columnList = "created_at")
})
public class OperationLog extends BaseEntity {

    @Column(name = "user_id", nullable = false)
    private Long userId;

    @Column(name = "username", nullable = false, length = 64)
    private String username;

    @Column(name = "ip_address", length = 64)
    private String ipAddress;

    @Column(name = "operation_type", nullable = false)
    private Integer operationType;

    @Column(name = "operation_detail", length = 512)
    private String operationDetail;

    @Column(name = "request_params", length = 2048)
    private String requestParams;

    @Column(name = "result", nullable = false)
    private Integer result;

    @Column(name = "error_msg", length = 1024)
    private String errorMsg;

    @Column(name = "execution_time")
    private Long executionTime;

    // Getters and Setters
}
```

#### 2.2.2 SystemLog（系统日志实体）

```java
@Entity
@Table(name = "system_log", indexes = {
    @Index(name = "idx_sys_log_level", columnList = "level"),
    @Index(name = "idx_sys_log_module", columnList = "module"),
    @Index(name = "idx_sys_log_created", columnList = "created_at")
})
public class SystemLog {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "level", nullable = false)
    private Integer level;

    @Column(name = "module", nullable = false, length = 64)
    private String module;

    @Column(name = "message", nullable = false, length = 2048)
    private String message;

    @Column(name = "stack_trace", length = 4096)
    private String stackTrace;

    @Column(name = "source_class", length = 256)
    private String sourceClass;

    @Column(name = "source_method", length = 128)
    private String sourceMethod;

    @Column(name = "thread_name", length = 64)
    private String threadName;

    @CreatedDate
    @Column(name = "created_at", updatable = false)
    private LocalDateTime createdAt;

    // Getters and Setters
}
```

### 2.3 Controller层设计

#### 2.3.1 OperationLogController

```java
@RestController
@RequestMapping("/api/v1/logs/operations")
@Tag(name = "操作日志管理", description = "操作日志查询接口")
@RequiredArgsConstructor
public class OperationLogController {

    private final OperationLogService operationLogService;

    /**
     * 分页查询操作日志
     *
     * @param request 查询条件
     * @return 分页结果
     */
    @GetMapping
    @Operation(summary = "操作日志分页查询", description = "支持按时间、用户、操作类型、结果筛选")
    @PreAuthorize("hasAnyRole('SUPER_ADMIN', 'SITE_ADMIN')")
    public Result<PageResult<OperationLogResponse>> queryOperationLogs(
            @Valid @ParameterObject OperationLogRequest request) {
        PageResult<OperationLogResponse> result = operationLogService.queryOperationLogs(request);
        return Result.success(result);
    }

    /**
     * 导出操作日志
     *
     * @param request 查询条件
     * @param response HTTP响应
     */
    @GetMapping("/export")
    @Operation(summary = "导出操作日志", description = "导出Excel格式的操作日志")
    @PreAuthorize("hasRole('SUPER_ADMIN')")
    public void exportOperationLogs(
            @Valid @ParameterObject OperationLogRequest request,
            HttpServletResponse response) {
        operationLogService.exportOperationLogs(request, response);
    }

    /**
     * 获取操作类型列表
     *
     * @return 操作类型枚举列表
     */
    @GetMapping("/operation-types")
    @Operation(summary = "获取操作类型列表")
    public Result<List<Map<String, Object>>> getOperationTypes() {
        return Result.success(operationLogService.getOperationTypes());
    }
}
```

#### 2.3.2 SystemLogController

```java
@RestController
@RequestMapping("/api/v1/logs/system")
@Tag(name = "系统日志管理", description = "系统日志查询接口")
@RequiredArgsConstructor
public class SystemLogController {

    private final SystemLogService systemLogService;

    /**
     * 分页查询系统日志
     *
     * @param request 查询条件
     * @return 分页结果
     */
    @GetMapping
    @Operation(summary = "系统日志分页查询", description = "支持按日志级别、时间范围筛选")
    @PreAuthorize("hasRole('SUPER_ADMIN')")
    public Result<PageResult<SystemLogResponse>> querySystemLogs(
            @Valid @ParameterObject SystemLogRequest request) {
        PageResult<SystemLogResponse> result = systemLogService.querySystemLogs(request);
        return Result.success(result);
    }

    /**
     * 获取日志级别列表
     *
     * @return 日志级别枚举列表
     */
    @GetMapping("/levels")
    @Operation(summary = "获取日志级别列表")
    public Result<List<Map<String, Object>>> getLogLevels() {
        return Result.success(systemLogService.getLogLevels());
    }
}
```

### 2.4 Service层设计

#### 2.4.1 OperationLogService

```java
public interface OperationLogService {

    /**
     * 分页查询操作日志
     */
    PageResult<OperationLogResponse> queryOperationLogs(OperationLogRequest request);

    /**
     * 异步保存操作日志
     */
    @Async("logExecutor")
    void saveOperationLogAsync(OperationLog log);

    /**
     * 导出操作日志
     */
    void exportOperationLogs(OperationLogRequest request, HttpServletResponse response);

    /**
     * 获取操作类型列表
     */
    List<Map<String, Object>> getOperationTypes();

    /**
     * 清理过期操作日志
     */
    int cleanupExpiredLogs(LocalDateTime before);
}
```

#### 2.4.2 OperationLogServiceImpl

```java
@Service
@RequiredArgsConstructor
@Slf4j
public class OperationLogServiceImpl implements OperationLogService {

    private final OperationLogRepository operationLogRepository;
    private final LogMapper logMapper;

    @Override
    public PageResult<OperationLogResponse> queryOperationLogs(OperationLogRequest request) {
        // 构建查询条件
        BooleanBuilder builder = new BooleanBuilder();

        // 站点隔离
        if (!SecurityUtils.isSuperAdmin()) {
            builder.and(QOperationLog.operationLog.siteId.eq(SecurityUtils.getCurrentSiteId()));
        }

        // 时间范围筛选
        if (request.getStartTime() != null) {
            builder.and(QOperationLog.operationLog.createdAt.goe(request.getStartTime()));
        }
        if (request.getEndTime() != null) {
            builder.and(QOperationLog.operationLog.createdAt.loe(request.getEndTime()));
        }

        // 用户筛选
        if (StringUtils.hasText(request.getUsername())) {
            builder.and(QOperationLog.operationLog.username.containsIgnoreCase(request.getUsername()));
        }

        // 操作类型筛选
        if (request.getOperationType() != null) {
            builder.and(QOperationLog.operationLog.operationType.eq(request.getOperationType()));
        }

        // 结果筛选
        if (request.getResult() != null) {
            builder.and(QOperationLog.operationLog.result.eq(request.getResult()));
        }

        // 执行分页查询
        Pageable pageable = PageRequest.of(
                request.getPage() - 1,
                request.getSize(),
                Sort.by(Sort.Direction.DESC, "createdAt")
        );

        Page<OperationLog> page = operationLogRepository.findAll(builder, pageable);

        // 转换并返回
        List<OperationLogResponse> list = page.getContent().stream()
                .map(logMapper::toResponse)
                .collect(Collectors.toList());

        return PageResult.of(list, page.getTotalElements(), page.getNumber() + 1, page.getSize());
    }

    @Override
    @Async("logExecutor")
    public void saveOperationLogAsync(OperationLog log) {
        try {
            operationLogRepository.save(log);
        } catch (Exception e) {
            log.error("保存操作日志失败", e);
        }
    }

    @Override
    public int cleanupExpiredLogs(LocalDateTime before) {
        return operationLogRepository.deleteByCreatedAtBefore(before);
    }

    // ... 其他方法实现
}
```

#### 2.4.3 SystemLogService

```java
public interface SystemLogService {

    /**
     * 分页查询系统日志
     */
    PageResult<SystemLogResponse> querySystemLogs(SystemLogRequest request);

    /**
     * 保存系统日志
     */
    void saveSystemLog(SystemLog log);

    /**
     * 获取日志级别列表
     */
    List<Map<String, Object>> getLogLevels();

    /**
     * 清理过期系统日志
     */
    int cleanupExpiredLogs(LocalDateTime before);
}
```

#### 2.4.4 LogCleanupService（日志清理定时任务）

```java
@Service
@RequiredArgsConstructor
@Slf4j
public class LogCleanupService {

    private final OperationLogService operationLogService;
    private final SystemLogService systemLogService;

    /**
     * 每天凌晨2点执行日志清理
     */
    @Scheduled(cron = "0 0 2 * * ?")
    public void cleanupLogs() {
        log.info("开始执行日志清理任务");

        try {
            // 清理180天前的操作日志
            LocalDateTime operationLogExpireTime = LocalDateTime.now().minusDays(180);
            int operationDeleted = operationLogService.cleanupExpiredLogs(operationLogExpireTime);
            log.info("清理操作日志完成，删除 {} 条记录", operationDeleted);

            // 清理30天前的系统日志
            LocalDateTime systemLogExpireTime = LocalDateTime.now().minusDays(30);
            int systemDeleted = systemLogService.cleanupExpiredLogs(systemLogExpireTime);
            log.info("清理系统日志完成，删除 {} 条记录", systemDeleted);

        } catch (Exception e) {
            log.error("日志清理任务执行失败", e);
        }
    }
}
```

### 2.5 DTO设计

#### 2.5.1 OperationLogRequest（操作日志查询请求）

```java
@Data
public class OperationLogRequest {

    @Schema(description = "页码", example = "1")
    @Min(value = 1, message = "页码必须大于0")
    private Integer page = 1;

    @Schema(description = "每页大小", example = "20")
    @Min(value = 1, message = "每页大小必须大于0")
    @Max(value = 100, message = "每页大小不能超过100")
    private Integer size = 20;

    @Schema(description = "开始时间")
    private LocalDateTime startTime;

    @Schema(description = "结束时间")
    private LocalDateTime endTime;

    @Schema(description = "用户名")
    private String username;

    @Schema(description = "操作类型：1-登录 2-登出 3-配置修改 4-用户管理 5-数据导出")
    private Integer operationType;

    @Schema(description = "操作结果：0-失败 1-成功")
    private Integer result;
}
```

#### 2.5.2 OperationLogResponse（操作日志查询响应）

```java
@Data
@Builder
public class OperationLogResponse {

    @Schema(description = "日志ID")
    private Long id;

    @Schema(description = "用户ID")
    private Long userId;

    @Schema(description = "用户名")
    private String username;

    @Schema(description = "IP地址")
    private String ipAddress;

    @Schema(description = "操作类型")
    private String operationType;

    @Schema(description = "操作类型编码")
    private Integer operationTypeCode;

    @Schema(description = "操作详情")
    private String operationDetail;

    @Schema(description = "请求参数")
    private String requestParams;

    @Schema(description = "操作结果：0-失败 1-成功")
    private Integer result;

    @Schema(description = "错误信息")
    private String errorMsg;

    @Schema(description = "执行时长(ms)")
    private Long executionTime;

    @Schema(description = "操作时间")
    private LocalDateTime createdAt;
}
```

#### 2.5.3 SystemLogRequest（系统日志查询请求）

```java
@Data
public class SystemLogRequest {

    @Schema(description = "页码", example = "1")
    @Min(value = 1, message = "页码必须大于0")
    private Integer page = 1;

    @Schema(description = "每页大小", example = "20")
    @Min(value = 1, message = "每页大小必须大于0")
    @Max(value = 100, message = "每页大小不能超过100")
    private Integer size = 20;

    @Schema(description = "日志级别：0-DEBUG 1-INFO 2-WARN 3-ERROR")
    private Integer level;

    @Schema(description = "模块名称")
    private String module;

    @Schema(description = "开始时间")
    private LocalDateTime startTime;

    @Schema(description = "结束时间")
    private LocalDateTime endTime;

    @Schema(description = "关键字搜索")
    private String keyword;
}
```

### 2.6 枚举类设计

#### 2.6.1 OperationType（操作类型枚举）

```java
@Getter
@AllArgsConstructor
public enum OperationType {

    LOGIN(1, "登录", "用户登录系统"),
    LOGOUT(2, "登出", "用户退出系统"),
    CONFIG_MODIFY(3, "配置修改", "修改系统配置"),
    USER_MANAGE(4, "用户管理", "创建/修改/删除用户"),
    ROLE_MANAGE(5, "角色管理", "创建/修改/删除角色"),
    DATA_EXPORT(6, "数据导出", "导出报表或数据"),
    ALARM_PROCESS(7, "报警处理", "处理报警记录"),
    DEVICE_MANAGE(8, "设备管理", "管理边缘盒子或通道"),
    THRESHOLD_CONFIG(9, "阈值配置", "修改算法阈值配置"),
    OTHER(99, "其他", "其他操作");

    private final Integer code;
    private final String name;
    private final String description;

    public static OperationType fromCode(Integer code) {
        for (OperationType type : values()) {
            if (type.code.equals(code)) {
                return type;
            }
        }
        return OTHER;
    }
}
```

#### 2.6.2 LogLevel（日志级别枚举）

```java
@Getter
@AllArgsConstructor
public enum LogLevel {

    DEBUG(0, "DEBUG", "调试信息"),
    INFO(1, "INFO", "普通信息"),
    WARN(2, "WARN", "警告信息"),
    ERROR(3, "ERROR", "错误信息");

    private final Integer code;
    private final String name;
    private final String description;

    public static LogLevel fromCode(Integer code) {
        for (LogLevel level : values()) {
            if (level.code.equals(code)) {
                return level;
            }
        }
        return INFO;
    }
}
```

### 2.7 Repository层设计

```java
@Repository
public interface OperationLogRepository extends JpaRepository<OperationLog, Long>,
        QuerydslPredicateExecutor<OperationLog> {

    /**
     * 批量删除指定时间之前的日志
     */
    @Modifying
    @Query("DELETE FROM OperationLog o WHERE o.createdAt < :before")
    int deleteByCreatedAtBefore(@Param("before") LocalDateTime before);

    /**
     * 统计指定用户的操作次数
     */
    long countByUserIdAndCreatedAtBetween(Long userId, LocalDateTime start, LocalDateTime end);
}

@Repository
public interface SystemLogRepository extends JpaRepository<SystemLog, Long>,
        QuerydslPredicateExecutor<SystemLog> {

    @Modifying
    @Query("DELETE FROM SystemLog s WHERE s.createdAt < :before")
    int deleteByCreatedAtBefore(@Param("before") LocalDateTime before);
}
```

---

## 3. 数据库表设计

### 3.1 sys_operation_log（操作日志表）

```sql
-- 操作日志表
CREATE TABLE sys_operation_log (
    id                  BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    site_id             BIGINT NOT NULL,
    user_id             BIGINT NOT NULL,
    username            VARCHAR(64) NOT NULL,
    ip_address          VARCHAR(64),
    operation_type      SMALLINT NOT NULL,
    operation_detail    VARCHAR(512),
    request_params      VARCHAR(2048),
    result              SMALLINT NOT NULL DEFAULT 1,
    error_msg           VARCHAR(1024),
    execution_time      BIGINT,
    created_at          TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at          TIMESTAMP,
    deleted_at          TIMESTAMP
);

-- 索引设计
CREATE INDEX idx_op_log_site_id ON sys_operation_log(site_id);
CREATE INDEX idx_op_log_user_id ON sys_operation_log(user_id);
CREATE INDEX idx_op_log_type ON sys_operation_log(operation_type);
CREATE INDEX idx_op_log_result ON sys_operation_log(result);
CREATE INDEX idx_op_log_created ON sys_operation_log(created_at);
CREATE INDEX idx_op_log_user_created ON sys_operation_log(user_id, created_at);

-- 表注释
COMMENT ON TABLE sys_operation_log IS '操作日志表';
COMMENT ON COLUMN sys_operation_log.id IS '主键ID';
COMMENT ON COLUMN sys_operation_log.site_id IS '站点ID';
COMMENT ON COLUMN sys_operation_log.user_id IS '用户ID';
COMMENT ON COLUMN sys_operation_log.username IS '用户名';
COMMENT ON COLUMN sys_operation_log.ip_address IS 'IP地址';
COMMENT ON COLUMN sys_operation_log.operation_type IS '操作类型：1-登录 2-登出 3-配置修改 4-用户管理 5-角色管理 6-数据导出 7-报警处理 8-设备管理 9-阈值配置 99-其他';
COMMENT ON COLUMN sys_operation_log.operation_detail IS '操作详情描述';
COMMENT ON COLUMN sys_operation_log.request_params IS '请求参数JSON';
COMMENT ON COLUMN sys_operation_log.result IS '操作结果：0-失败 1-成功';
COMMENT ON COLUMN sys_operation_log.error_msg IS '错误信息';
COMMENT ON COLUMN sys_operation_log.execution_time IS '执行时长(毫秒)';
COMMENT ON COLUMN sys_operation_log.created_at IS '创建时间';
```

### 3.2 system_log（系统日志表）

```sql
-- 系统日志表
CREATE TABLE system_log (
    id                  BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    level               SMALLINT NOT NULL,
    module              VARCHAR(64) NOT NULL,
    message             VARCHAR(2048) NOT NULL,
    stack_trace         VARCHAR(4096),
    source_class        VARCHAR(256),
    source_method       VARCHAR(128),
    thread_name         VARCHAR(64),
    created_at          TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引设计
CREATE INDEX idx_sys_log_level ON system_log(level);
CREATE INDEX idx_sys_log_module ON system_log(module);
CREATE INDEX idx_sys_log_created ON system_log(created_at);
CREATE INDEX idx_sys_log_level_created ON system_log(level, created_at);

-- 表注释
COMMENT ON TABLE system_log IS '系统日志表';
COMMENT ON COLUMN system_log.id IS '主键ID';
COMMENT ON COLUMN system_log.level IS '日志级别：0-DEBUG 1-INFO 2-WARN 3-ERROR';
COMMENT ON COLUMN system_log.module IS '模块名称';
COMMENT ON COLUMN system_log.message IS '日志消息';
COMMENT ON COLUMN system_log.stack_trace IS '异常堆栈';
COMMENT ON COLUMN system_log.source_class IS '来源类名';
COMMENT ON COLUMN system_log.source_method IS '来源方法名';
COMMENT ON COLUMN system_log.thread_name IS '线程名称';
COMMENT ON COLUMN system_log.created_at IS '创建时间';
```

---

## 4. 接口详细设计

### 4.1 接口清单

| 方法 | 路径 | 说明 | 权限要求 |
|------|------|------|----------|
| GET | `/api/v1/logs/operations` | 操作日志分页查询 | SUPER_ADMIN, SITE_ADMIN |
| GET | `/api/v1/logs/operations/export` | 导出操作日志 | SUPER_ADMIN |
| GET | `/api/v1/logs/operations/operation-types` | 获取操作类型列表 | SUPER_ADMIN, SITE_ADMIN |
| GET | `/api/v1/logs/system` | 系统日志分页查询 | SUPER_ADMIN |
| GET | `/api/v1/logs/system/levels` | 获取日志级别列表 | SUPER_ADMIN |

### 4.2 操作日志查询接口

#### 请求

```
GET /api/v1/logs/operations?page=1&size=20&startTime=2026-03-01T00:00:00&endTime=2026-03-29T23:59:59&operationType=1&result=1
```

#### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页大小，默认20，最大100 |
| startTime | DateTime | 否 | 开始时间，ISO 8601格式 |
| endTime | DateTime | 否 | 结束时间，ISO 8601格式 |
| username | String | 否 | 用户名，模糊匹配 |
| operationType | Integer | 否 | 操作类型：1-登录 2-登出 3-配置修改... |
| result | Integer | 否 | 操作结果：0-失败 1-成功 |

#### 响应

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 10001,
        "userId": 1,
        "username": "admin",
        "ipAddress": "192.168.1.100",
        "operationType": "登录",
        "operationTypeCode": 1,
        "operationDetail": "用户登录系统",
        "requestParams": "{\"username\":\"admin\"}",
        "result": 1,
        "errorMsg": null,
        "executionTime": 125,
        "createdAt": "2026-03-29T10:23:15"
      },
      {
        "id": 10002,
        "userId": 1,
        "username": "admin",
        "ipAddress": "192.168.1.100",
        "operationType": "配置修改",
        "operationTypeCode": 3,
        "operationDetail": "修改PDI标准工时配置",
        "requestParams": "{\"door\":\"left_front\",\"duration\":15}",
        "result": 1,
        "errorMsg": null,
        "executionTime": 45,
        "createdAt": "2026-03-29T10:25:30"
      }
    ],
    "total": 156,
    "page": 1,
    "size": 20
  },
  "timestamp": 1711699200000
}
```

### 4.3 系统日志查询接口

#### 请求

```
GET /api/v1/logs/system?page=1&size=20&level=3&startTime=2026-03-01T00:00:00&endTime=2026-03-29T23:59:59
```

#### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页大小，默认20，最大100 |
| level | Integer | 否 | 日志级别：0-DEBUG 1-INFO 2-WARN 3-ERROR |
| module | String | 否 | 模块名称 |
| startTime | DateTime | 否 | 开始时间 |
| endTime | DateTime | 否 | 结束时间 |
| keyword | String | 否 | 关键字搜索 |

#### 响应

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 5001,
        "level": "ERROR",
        "levelCode": 3,
        "module": "algorithm-inlet",
        "message": "接收状态流数据失败：连接超时",
        "stackTrace": "java.net.SocketTimeoutException: Read timed out\n    at java.base/...",
        "sourceClass": "com.vdc.pdi.algorithm.service.StateReceiver",
        "sourceMethod": "receiveState",
        "threadName": "http-nio-8080-exec-5",
        "createdAt": "2026-03-29T10:23:15"
      },
      {
        "id": 5002,
        "level": "INFO",
        "levelCode": 1,
        "module": "log-cleanup",
        "message": "日志清理任务执行完成，删除操作日志 125 条",
        "stackTrace": null,
        "sourceClass": "com.vdc.pdi.log.service.LogCleanupService",
        "sourceMethod": "cleanupLogs",
        "threadName": "scheduling-1",
        "createdAt": "2026-03-29T02:00:05"
      }
    ],
    "total": 89,
    "page": 1,
    "size": 20
  },
  "timestamp": 1711699200000
}
```

---

## 5. AOP日志记录设计

### 5.1 @OperationLog注解

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface OperationLog {

    /**
     * 操作类型
     */
    OperationType type();

    /**
     * 操作描述
     */
    String description() default "";

    /**
     * 是否记录请求参数
     */
    boolean recordParams() default true;

    /**
     * 参数过滤字段（不记录敏感字段）
     */
    String[] excludeParams() default {"password", "token", "secret"};
}
```

### 5.2 OperationLogAspect切面

```java
@Aspect
@Component
@RequiredArgsConstructor
@Slf4j
public class OperationLogAspect {

    private final OperationLogService operationLogService;

    /**
     * 定义切点：所有带有@OperationLog注解的方法
     */
    @Pointcut("@annotation(com.vdc.pdi.log.annotation.OperationLog)")
    public void operationLogPointcut() {
    }

    /**
     * 环绕通知：记录操作日志
     */
    @Around("operationLogPointcut()")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        // 获取方法签名和注解
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        Method method = signature.getMethod();
        OperationLog annotation = method.getAnnotation(OperationLog.class);

        // 记录开始时间
        long startTime = System.currentTimeMillis();

        // 获取当前用户和IP
        Long userId = SecurityUtils.getCurrentUserId();
        String username = SecurityUtils.getCurrentUsername();
        String ipAddress = ServletUtils.getClientIP();

        // 构建操作日志
        OperationLogEntity operationLog = new OperationLogEntity();
        operationLog.setUserId(userId);
        operationLog.setUsername(username);
        operationLog.setIpAddress(ipAddress);
        operationLog.setOperationType(annotation.type().getCode());
        operationLog.setOperationDetail(buildDescription(annotation, joinPoint));

        // 记录请求参数
        if (annotation.recordParams()) {
            String params = buildRequestParams(joinPoint, annotation.excludeParams());
            operationLog.setRequestParams(params);
        }

        // 执行目标方法
        Object result;
        try {
            result = joinPoint.proceed();
            // 执行成功
            operationLog.setResult(1);
        } catch (Exception e) {
            // 执行失败
            operationLog.setResult(0);
            operationLog.setErrorMsg(StringUtils.substring(e.getMessage(), 0, 1024));
            throw e;
        } finally {
            // 计算执行时长
            long executionTime = System.currentTimeMillis() - startTime;
            operationLog.setExecutionTime(executionTime);

            // 异步保存日志
            operationLogService.saveOperationLogAsync(operationLog);
        }

        return result;
    }

    /**
     * 构建操作描述
     */
    private String buildDescription(OperationLog annotation, ProceedingJoinPoint joinPoint) {
        String description = annotation.description();
        if (StringUtils.hasText(description)) {
            return description;
        }
        return annotation.type().getDescription();
    }

    /**
     * 构建请求参数字符串
     */
    private String buildRequestParams(ProceedingJoinPoint joinPoint, String[] excludeParams) {
        try {
            Object[] args = joinPoint.getArgs();
            if (args == null || args.length == 0) {
                return "{}";
            }

            // 过滤敏感参数
            Map<String, Object> params = new HashMap<>();
            for (Object arg : args) {
                if (arg == null) {
                    continue;
                }
                // 忽略HttpServletRequest/Response等类型
                if (arg instanceof HttpServletRequest ||
                    arg instanceof HttpServletResponse ||
                    arg instanceof MultipartFile) {
                    continue;
                }

                // 转换为Map并过滤敏感字段
                Map<String, Object> argMap = JsonUtils.toMap(arg);
                for (String exclude : excludeParams) {
                    argMap.remove(exclude);
                }
                params.putAll(argMap);
            }

            String json = JsonUtils.toJson(params);
            // 限制长度
            return StringUtils.substring(json, 0, 2048);
        } catch (Exception e) {
            log.warn("构建请求参数失败", e);
            return "{}";
        }
    }
}
```

### 5.3 异步配置

```java
@Configuration
@EnableAsync
public class AsyncConfig {

    /**
     * 日志写入专用线程池
     */
    @Bean("logExecutor")
    public Executor logExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(5);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("log-async-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setWaitForTasksToCompleteOnShutdown(true);
        executor.setAwaitTerminationSeconds(60);
        executor.initialize();
        return executor;
    }
}
```

### 5.4 使用示例

```java
@RestController
@RequestMapping("/api/v1/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthService authService;

    @PostMapping("/login")
    @OperationLog(type = OperationType.LOGIN, description = "用户登录")
    public Result<LoginResponse> login(@RequestBody @Valid LoginRequest request) {
        return Result.success(authService.login(request));
    }

    @PostMapping("/logout")
    @OperationLog(type = OperationType.LOGOUT)
    public Result<Void> logout() {
        authService.logout();
        return Result.success();
    }
}

@RestController
@RequestMapping("/api/v1/config")
@RequiredArgsConstructor
public class SystemConfigController {

    private final SystemConfigService configService;

    @PutMapping("/threshold")
    @OperationLog(type = OperationType.THRESHOLD_CONFIG,
                  description = "修改算法阈值配置",
                  excludeParams = {"secretKey"})
    public Result<Void> updateThreshold(@RequestBody @Valid ThresholdConfigRequest request) {
        configService.updateThreshold(request);
        return Result.success();
    }
}
```

---

## 6. 日志清理策略

### 6.1 清理规则

| 日志类型 | 保留期限 | 清理时间 |
|----------|----------|----------|
| 操作日志 | 180天 | 每天凌晨2:00 |
| 系统日志 | 30天 | 每天凌晨2:00 |

### 6.2 定时任务配置

```java
@Configuration
@EnableScheduling
public class SchedulingConfig {

    /**
     * 配置任务调度器线程池
     */
    @Bean
    public TaskScheduler taskScheduler() {
        ThreadPoolTaskScheduler scheduler = new ThreadPoolTaskScheduler();
        scheduler.setPoolSize(5);
        scheduler.setThreadNamePrefix("scheduled-");
        scheduler.setAwaitTerminationSeconds(60);
        scheduler.setWaitForTasksToCompleteOnShutdown(true);
        return scheduler;
    }
}
```

### 6.3 清理策略说明

1. **批量删除**：使用JPQL批量删除语句，避免内存溢出
2. **异步执行**：日志清理在独立线程执行，不影响主业务
3. **失败重试**：清理失败会记录错误日志，下次调度继续执行
4. **手动触发**：预留手动触发接口，便于紧急清理

```java
@RestController
@RequestMapping("/api/v1/logs/admin")
@RequiredArgsConstructor
public class LogAdminController {

    private final LogCleanupService logCleanupService;

    @PostMapping("/cleanup")
    @OperationLog(type = OperationType.OTHER, description = "手动触发日志清理")
    @PreAuthorize("hasRole('SUPER_ADMIN')")
    public Result<Map<String, Integer>> manualCleanup() {
        logCleanupService.cleanupLogs();
        // 返回清理结果
        Map<String, Integer> result = new HashMap<>();
        result.put("operationLogsDeleted", operationCount);
        result.put("systemLogsDeleted", systemCount);
        return Result.success(result);
    }
}
```

---

## 7. 单元测试用例设计

### 7.1 测试目录结构

```
src/test/java/com/vdc/pdi/logmanagement/
├── controller/
│   ├── OperationLogControllerTest.java
│   └── SystemLogControllerTest.java
├── service/
│   ├── OperationLogServiceTest.java
│   ├── SystemLogServiceTest.java
│   └── LogCleanupServiceTest.java
├── aspect/
│   └── OperationLogAspectTest.java
└── integration/
    └── LogManagementIntegrationTest.java
```

### 7.2 测试用例清单

#### 7.2.1 OperationLogControllerTest

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| OC-001 | 正常分页查询操作日志 | 返回分页结果，状态码200 |
| OC-002 | 按时间范围筛选查询 | 返回符合条件的记录 |
| OC-003 | 按操作类型筛选查询 | 返回指定类型的记录 |
| OC-004 | 按用户名模糊查询 | 返回匹配用户名的记录 |
| OC-005 | 按操作结果筛选 | 返回成功/失败的记录 |
| OC-006 | 组合条件查询 | 返回同时满足所有条件的记录 |
| OC-007 | 越权访问（普通用户） | 返回403禁止访问 |
| OC-008 | 导出操作日志 | 返回Excel文件流 |
| OC-009 | 获取操作类型列表 | 返回所有操作类型枚举 |
| OC-010 | 分页参数校验（page=0） | 返回400参数错误 |

#### 7.2.2 SystemLogControllerTest

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| SC-001 | 正常分页查询系统日志 | 返回分页结果，状态码200 |
| SC-002 | 按日志级别筛选 | 返回指定级别的记录 |
| SC-003 | 按模块名称筛选 | 返回指定模块的记录 |
| SC-004 | 关键字搜索 | 返回消息包含关键字的记录 |
| SC-005 | 越权访问（站点管理员） | 返回403禁止访问 |

#### 7.2.3 OperationLogServiceTest

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| OS-001 | 保存操作日志 | 数据成功写入数据库 |
| OS-002 | 异步保存操作日志 | 任务提交到线程池执行 |
| OS-003 | 查询操作日志（带站点隔离） | 只返回当前站点数据 |
| OS-004 | 超级管理员查询（跨站点） | 返回所有站点数据 |
| OS-005 | 清理过期操作日志 | 删除指定时间前的记录 |
| OS-006 | 保存超长请求参数 | 参数被截断至2048字符 |
| OS-007 | 保存超长错误信息 | 错误信息被截断至1024字符 |

#### 7.2.4 LogCleanupServiceTest

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| LC-001 | 正常执行日志清理 | 删除过期记录，记录日志 |
| LC-002 | 无可清理数据 | 正常完成，删除0条 |
| LC-003 | 清理执行异常 | 记录错误日志，不抛出异常 |
| LC-004 | 大量数据清理 | 分批处理，不阻塞 |

#### 7.2.5 OperationLogAspectTest

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| OA-001 | 正常方法执行记录日志 | 记录成功日志，包含执行时长 |
| OA-002 | 方法执行异常记录日志 | 记录失败日志，包含错误信息 |
| OA-003 | 过滤敏感参数 | 密码等字段不被记录 |
| OA-004 | 自定义操作描述 | 使用注解中指定的描述 |
| OA-005 | 禁用参数记录 | 不记录请求参数 |

### 7.3 测试代码示例

```java
@WebMvcTest(OperationLogController.class)
@AutoConfigureMockMvc(addFilters = false)
class OperationLogControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private OperationLogService operationLogService;

    @Test
    @WithMockUser(roles = "SUPER_ADMIN")
    void testQueryOperationLogs_Success() throws Exception {
        // 准备测试数据
        PageResult<OperationLogResponse> pageResult = PageResult.of(
            Collections.singletonList(createMockLog()),
            1L, 1, 20
        );
        when(operationLogService.queryOperationLogs(any())).thenReturn(pageResult);

        // 执行请求并验证
        mockMvc.perform(get("/api/v1/logs/operations")
                .param("page", "1")
                .param("size", "20"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200))
            .andExpect(jsonPath("$.data.total").value(1));
    }

    @Test
    @WithMockUser(roles = "NORMAL_USER")
    void testQueryOperationLogs_Forbidden() throws Exception {
        mockMvc.perform(get("/api/v1/logs/operations"))
            .andExpect(status().isForbidden());
    }

    private OperationLogResponse createMockLog() {
        return OperationLogResponse.builder()
            .id(1L)
            .username("admin")
            .operationType("登录")
            .result(1)
            .build();
    }
}

@SpringBootTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.ANY)
class LogCleanupServiceTest {

    @Autowired
    private LogCleanupService logCleanupService;

    @Autowired
    private OperationLogRepository operationLogRepository;

    @Test
    void testCleanupExpiredLogs() {
        // 准备过期数据
        LocalDateTime oldTime = LocalDateTime.now().minusDays(200);
        OperationLog oldLog = new OperationLog();
        oldLog.setCreatedAt(oldTime);
        operationLogRepository.save(oldLog);

        // 准备有效数据
        OperationLog newLog = new OperationLog();
        newLog.setCreatedAt(LocalDateTime.now());
        operationLogRepository.save(newLog);

        // 执行清理
        int deleted = logCleanupService.cleanupExpiredLogs(
            LocalDateTime.now().minusDays(180)
        );

        // 验证结果
        assertThat(deleted).isEqualTo(1);
        assertThat(operationLogRepository.count()).isEqualTo(1);
    }
}
```

---

## 附录

### A. 模块间交互时序图

```
┌─────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  用户   │    │  Controller │    │   Aspect    │    │   Service   │
└────┬────┘    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘
     │                │                  │                  │
     │  1. 请求操作    │                  │                  │
     │────────────────>│                  │                  │
     │                │                  │                  │
     │                │  2. 进入切面      │                  │
     │                │─────────────────>│                  │
     │                │                  │                  │
     │                │                  │  3. 记录开始时间   │
     │                │                  │  4. 获取用户信息   │
     │                │                  │                  │
     │                │  5. 执行目标方法   │                  │
     │                │<─────────────────│                  │
     │                │                  │                  │
     │                │  6. 调用Service   │                  │
     │                │─────────────────────────────────────>│
     │                │                  │                  │
     │                │  7. 返回结果      │                  │
     │                │<─────────────────────────────────────│
     │                │                  │                  │
     │                │  8. 返回切面      │                  │
     │                │─────────────────>│                  │
     │                │                  │                  │
     │                │                  │  9. 计算执行时长   │
     │                │                  │  10. 异步保存日志  │
     │                │                  │─────────────────>│
     │                │                  │                  │
     │  11. 返回响应   │                  │                  │
     │<────────────────│                  │                  │
     │                │                  │                  │
```

### B. 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0 | 2026-03-29 | 后端程序员-G | 初始版本 |

---

**文档结束**
