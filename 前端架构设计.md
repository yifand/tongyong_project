# PDI智能监测平台 - 前端架构设计文档

## 1. 技术选型

### 1.1 核心技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.4.x | 渐进式JavaScript框架，使用Composition API |
| TypeScript | 5.3.x | 类型安全的JavaScript超集 |
| Element Plus | 2.5.x | Vue3组件库，支持暗黑主题 |
| Vite | 5.0.x | 下一代前端构建工具 |
| Pinia | 2.1.x | Vue3官方状态管理方案 |
| Vue Router | 4.2.x | 官方路由管理器 |
| Axios | 1.6.x | HTTP客户端 |

### 1.2 开发工具链

```bash
# 代码规范
npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin
npm install -D prettier eslint-config-prettier eslint-plugin-prettier
npm install -D stylelint stylelint-config-standard stylelint-config-recommended-vue

# 提交规范
npm install -D husky lint-staged @commitlint/cli @commitlint/config-conventional

# 类型定义
npm install -D @types/node vue-tsc
```

### 1.3 ESLint配置

```javascript
// .eslintrc.cjs
module.exports = {
  root: true,
  env: {
    browser: true,
    es2021: true,
    node: true
  },
  extends: [
    'eslint:recommended',
    '@vue/typescript/recommended',
    'plugin:vue/vue3-recommended',
    'plugin:prettier/recommended'
  ],
  parser: 'vue-eslint-parser',
  parserOptions: {
    ecmaVersion: 'latest',
    parser: '@typescript-eslint/parser',
    sourceType: 'module'
  },
  rules: {
    'vue/multi-word-component-names': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',
    '@typescript-eslint/no-unused-vars': 'error',
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'error' : 'off'
  }
}
```

### 1.4 Prettier配置

```javascript
// .prettierrc.cjs
module.exports = {
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'none',
  printWidth: 100,
  arrowParens: 'avoid',
  endOfLine: 'lf',
  vueIndentScriptAndStyle: true
}
```

---

## 2. 项目结构

```
pdi-frontend/
├── public/                     # 静态资源（不经过构建）
│   ├── favicon.ico
│   └── alarm-sound.mp3         # 报警音效
├── src/
│   ├── api/                    # API接口层
│   │   ├── modules/            # 按模块组织的API
│   │   │   ├── auth.ts         # 认证相关
│   │   │   ├── alarm.ts        # 预警相关
│   │   │   ├── device.ts       # 设备相关
│   │   │   ├── archive.ts      # 档案相关
│   │   │   └── system.ts       # 系统管理
│   │   ├── request.ts          # Axios封装
│   │   └── types.ts            # API类型定义
│   │
│   ├── assets/                 # 静态资源
│   │   ├── images/             # 图片资源
│   │   ├── icons/              # SVG图标
│   │   └── styles/             # 样式文件
│   │       ├── variables.scss  # SCSS变量
│   │       └── index.scss      # 全局样式入口
│   │
│   ├── components/             # 公共组件（原子设计）
│   │   ├── atoms/              # 原子组件
│   │   │   ├── AppButton/
│   │   │   ├── AppIcon/
│   │   │   ├── AppBadge/
│   │   │   └── AppTag/
│   │   ├── molecules/          # 分子组件
│   │   │   ├── SearchForm/
│   │   │   ├── DataTable/
│   │   │   ├── StatisticCard/
│   │   │   └── StatusTag/
│   │   ├── organisms/          # 有机体组件
│   │   │   ├── AppHeader/
│   │   │   ├── AppSidebar/
│   │   │   ├── AlarmCard/
│   │   │   └── DeviceCard/
│   │   └── templates/          # 模板组件
│   │       ├── PageContainer/
│   │       ├── DetailLayout/
│   │       └── ListLayout/
│   │
│   ├── composables/            # 组合式函数
│   │   ├── useAuth.ts          # 权限相关
│   │   ├── useAlarm.ts         # 报警相关
│   │   ├── useDevice.ts        # 设备相关
│   │   ├── useWebSocket.ts     # WebSocket封装
│   │   ├── useTable.ts         # 表格逻辑
│   │   └── useTheme.ts         # 主题切换
│   │
│   ├── directives/             # 自定义指令
│   │   ├── permission.ts       # 权限指令
│   │   ├── debounce.ts         # 防抖指令
│   │   └── copy.ts             # 复制指令
│   │
│   ├── layouts/                # 布局组件
│   │   ├── DefaultLayout.vue   # 默认布局（侧边栏+头部）
│   │   ├── FullscreenLayout.vue # 全屏布局（实时监控）
│   │   └── AuthLayout.vue      # 认证布局（登录页）
│   │
│   ├── router/                 # 路由配置
│   │   ├── index.ts            # 路由入口
│   │   ├── routes.ts           # 静态路由
│   │   ├── dynamic-routes.ts   # 动态路由（权限生成）
│   │   └── guards.ts           # 路由守卫
│   │
│   ├── stores/                 # Pinia状态管理
│   │   ├── index.ts            # Store入口
│   │   ├── modules/
│   │   │   ├── user.ts         # 用户信息
│   │   │   ├── app.ts          # 应用配置
│   │   │   ├── alarm.ts        # 报警状态
│   │   │   ├── device.ts       # 设备状态
│   │   │   └── permission.ts   # 权限状态
│   │   └── plugins/
│   │       └── persist.ts      # 持久化插件
│   │
│   ├── styles/                 # 全局样式
│   │   ├── index.scss          # 样式入口
│   │   ├── dark-theme.scss     # 暗黑主题变量
│   │   ├── element-override.scss # ElementPlus覆盖
│   │   ├── transition.scss     # 过渡动画
│   │   └── mixins.scss         # SCSS混入
│   │
│   ├── types/                  # TypeScript类型
│   │   ├── global.d.ts         # 全局类型
│   │   ├── api.d.ts            # API类型
│   │   ├── components.d.ts     # 组件类型
│   │   └── store.d.ts          # Store类型
│   │
│   ├── utils/                  # 工具函数
│   │   ├── index.ts            # 工具入口
│   │   ├── storage.ts          # 本地存储封装
│   │   ├── date.ts             # 日期处理
│   │   ├── validate.ts         # 表单验证
│   │   ├── format.ts           # 数据格式化
│   │   └── constants.ts        # 常量定义
│   │
│   ├── views/                  # 页面视图
│   │   ├── login/              # 登录页
│   │   ├── dashboard/          # 工作台
│   │   ├── alarm/              # 预警中心
│   │   │   ├── realtime/
│   │   │   └── history/
│   │   ├── archive/            # 行为档案
│   │   ├── device/             # 设备管理
│   │   │   ├── box/
│   │   │   └── channel/
│   │   └── system/             # 系统管理
│   │       ├── user/
│   │       ├── role/
│   │       ├── config/
│   │       └── log/
│   │
│   ├── App.vue                 # 根组件
│   └── main.ts                 # 应用入口
│
├── .env                        # 环境变量
├── .env.development
├── .env.production
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 3. 架构设计详情

### 3.1 组件设计规范

#### 3.1.1 原子设计方法论

```
atoms/       → 最小不可再分组件（Button, Icon, Badge）
molecules/   → 原子组合（SearchForm = Input + Button + Select）
organisms/   → 分子组合（AlarmCard = Badge + Image + Button）
templates/   → 页面布局骨架
views/       → 具体业务页面
```

#### 3.1.2 组件命名规范

```typescript
// 文件命名：PascalCase
// AppButton.vue, DataTable.vue, AlarmDetailDialog.vue

// 组件命名
export default defineComponent({
  name: 'AppButton'  // 多单词，避免与HTML标签冲突
})

//  Props命名：camelCase
interface Props {
  alarmType: string
  alarmLevel: number
  isReadonly?: boolean
}

// 事件命名：kebab-case
// @alarm-confirmed, @page-changed, @search-submitted

// 插槽命名：kebab-case
// <slot name="action-buttons" />
```

#### 3.1.3 组件模板示例

```vue
<!-- organisms/AlarmCard/AlarmCard.vue -->
<template>
  <div class="alarm-card" :class="{ 'is-urgent': isUrgent }">
    <div class="alarm-card__header">
      <AppBadge :type="alarmLevelType" :text="alarmLevelText" />
      <span class="alarm-card__time">{{ formattedTime }}</span>
    </div>
    <div class="alarm-card__image" @click="handleImageClick">
      <img :src="alarmImage" :alt="alarmType" />
      <div class="alarm-card__overlay">
        <AppIcon name="zoom-in" />
      </div>
    </div>
    <div class="alarm-card__info">
      <h4 class="alarm-card__title">{{ alarmTitle }}</h4>
      <p class="alarm-card__location">
        <AppIcon name="location" />
        {{ location }}
      </p>
    </div>
    <div class="alarm-card__actions">
      <slot name="actions">
        <AppButton type="primary" @click="handleConfirm">确认</AppButton>
        <AppButton @click="handleViewDetail">详情</AppButton>
      </slot>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import AppBadge from '@/components/atoms/AppBadge/AppBadge.vue'
import AppButton from '@/components/atoms/AppButton/AppButton.vue'
import AppIcon from '@/components/atoms/AppIcon/AppIcon.vue'
import { formatDateTime } from '@/utils/date'

interface Props {
  alarmData: AlarmItem
}

const props = defineProps<Props>()
const emit = defineEmits<{
  confirm: [id: string]
  viewDetail: [id: string]
  imageClick: [url: string]
}>()

// 计算属性
const isUrgent = computed(() => props.alarmData.level === 1)
const alarmLevelType = computed(() => {
  const map: Record<number, string> = { 1: 'danger', 2: 'warning', 3: 'info' }
  return map[props.alarmData.level]
})
const formattedTime = computed(() => formatDateTime(props.alarmData.createTime))

// 方法
const handleConfirm = () => emit('confirm', props.alarmData.id)
const handleViewDetail = () => emit('viewDetail', props.alarmData.id)
const handleImageClick = () => emit('imageClick', props.alarmData.imageUrl)
</script>

<style scoped lang="scss">
.alarm-card {
  background: var(--el-bg-color-overlay);
  border-radius: 8px;
  overflow: hidden;
  transition: box-shadow 0.3s;

  &.is-urgent {
    border: 1px solid var(--el-color-danger);
  }

  &__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 16px;
  }

  // ... 其他样式
}
</style>
```

### 3.2 状态管理设计

#### 3.2.1 Store模块划分

```typescript
// stores/modules/user.ts
import { defineStore } from 'pinia'
import { login, getUserInfo } from '@/api/modules/auth'
import type { UserInfo, LoginForm } from '@/types/api'

export const useUserStore = defineStore('user', () => {
  // State
  const token = ref<string>(localStorage.getItem('token') || '')
  const userInfo = ref<UserInfo | null>(null)
  const permissions = ref<string[]>([])
  const roles = ref<string[]>([])

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const username = computed(() => userInfo.value?.username || '')
  const siteId = computed(() => userInfo.value?.siteId || '')

  // Actions
  async function loginAction(form: LoginForm) {
    const { data } = await login(form)
    token.value = data.token
    localStorage.setItem('token', data.token)
    return data
  }

  async function fetchUserInfo() {
    const { data } = await getUserInfo()
    userInfo.value = data
    permissions.value = data.permissions
    roles.value = data.roles
    return data
  }

  function logout() {
    token.value = ''
    userInfo.value = null
    permissions.value = []
    roles.value = []
    localStorage.removeItem('token')
  }

  return {
    token,
    userInfo,
    permissions,
    roles,
    isLoggedIn,
    username,
    siteId,
    loginAction,
    fetchUserInfo,
    logout
  }
})
```

```typescript
// stores/modules/alarm.ts
import { defineStore } from 'pinia'
import { getRealtimeAlarms, getAlarmHistory, confirmAlarm } from '@/api/modules/alarm'
import type { AlarmItem, AlarmQueryParams } from '@/types/api'

export const useAlarmStore = defineStore('alarm', () => {
  // State
  const realtimeAlarms = ref<AlarmItem[]>([])
  const unreadCount = ref(0)
  const currentAlarm = ref<AlarmItem | null>(null)
  const isSoundEnabled = ref(true)
  const isAlarmDialogVisible = ref(false)

  // Getters
  const urgentAlarms = computed(() =>
    realtimeAlarms.value.filter(a => a.level === 1 && !a.isConfirmed)
  )
  const hasUnread = computed(() => unreadCount.value > 0)

  // Actions
  function addRealtimeAlarm(alarm: AlarmItem) {
    realtimeAlarms.value.unshift(alarm)
    unreadCount.value++

    // 限制列表长度
    if (realtimeAlarms.value.length > 100) {
      realtimeAlarms.value.pop()
    }

    // 播放音效
    if (isSoundEnabled.value && alarm.level === 1) {
      playAlarmSound()
    }
  }

  function showAlarmDialog(alarm: AlarmItem) {
    currentAlarm.value = alarm
    isAlarmDialogVisible.value = true
  }

  async function confirmAlarmAction(id: string) {
    await confirmAlarm(id)
    const alarm = realtimeAlarms.value.find(a => a.id === id)
    if (alarm) {
      alarm.isConfirmed = true
    }
  }

  function playAlarmSound() {
    const audio = new Audio('/alarm-sound.mp3')
    audio.play().catch(() => {})
  }

  return {
    realtimeAlarms,
    unreadCount,
    currentAlarm,
    isSoundEnabled,
    isAlarmDialogVisible,
    urgentAlarms,
    hasUnread,
    addRealtimeAlarm,
    showAlarmDialog,
    confirmAlarmAction,
    playAlarmSound
  }
})
```

```typescript
// stores/modules/device.ts
import { defineStore } from 'pinia'
import { getDeviceList, getDeviceStatus } from '@/api/modules/device'
import type { Device, DeviceStatus } from '@/types/api'

export const useDeviceStore = defineStore('device', () => {
  // State
  const devices = ref<Device[]>([])
  const onlineCount = ref(0)
  const offlineCount = ref(0)
  const loading = ref(false)

  // Getters
  const onlineRate = computed(() => {
    const total = devices.value.length
    return total > 0 ? Math.round((onlineCount.value / total) * 100) : 0
  })

  const deviceOptions = computed(() =>
    devices.value.map(d => ({ label: d.name, value: d.id }))
  )

  // Actions
  async function fetchDevices() {
    loading.value = true
    try {
      const { data } = await getDeviceList()
      devices.value = data.list
      onlineCount.value = data.list.filter(d => d.status === 'online').length
      offlineCount.value = data.list.filter(d => d.status === 'offline').length
    } finally {
      loading.value = false
    }
  }

  function updateDeviceStatus(deviceId: string, status: DeviceStatus) {
    const device = devices.value.find(d => d.id === deviceId)
    if (device) {
      const oldStatus = device.status
      device.status = status

      // 更新在线/离线计数
      if (oldStatus !== status) {
        if (status === 'online') {
          onlineCount.value++
          offlineCount.value--
        } else {
          onlineCount.value--
          offlineCount.value++
        }
      }
    }
  }

  return {
    devices,
    onlineCount,
    offlineCount,
    loading,
    onlineRate,
    deviceOptions,
    fetchDevices,
    updateDeviceStatus
  }
})
```

### 3.3 路由设计

#### 3.3.1 路由配置

```typescript
// router/routes.ts
import type { RouteRecordRaw } from 'vue-router'

export const constantRoutes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { hidden: true, title: '登录' }
  },
  {
    path: '/404',
    name: '404',
    component: () => import('@/views/error/404.vue'),
    meta: { hidden: true }
  }
]

export const asyncRoutes: RouteRecordRaw[] = [
  {
    path: '/',
    component: () => import('@/layouts/DefaultLayout.vue'),
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/dashboard/index.vue'),
        meta: {
          title: '工作台',
          icon: 'dashboard',
          affix: true
        }
      }
    ]
  },
  {
    path: '/alarm',
    component: () => import('@/layouts/DefaultLayout.vue'),
    redirect: '/alarm/realtime',
    meta: { title: '预警中心', icon: 'alarm' },
    children: [
      {
        path: 'realtime',
        name: 'AlarmRealtime',
        component: () => import('@/views/alarm/realtime/index.vue'),
        meta: {
          title: '实时预警',
          icon: 'video',
          permissions: ['alarm:realtime:view']
        }
      },
      {
        path: 'history',
        name: 'AlarmHistory',
        component: () => import('@/views/alarm/history/index.vue'),
        meta: {
          title: '历史预警',
          icon: 'history',
          permissions: ['alarm:history:view']
        }
      }
    ]
  },
  {
    path: '/archive',
    component: () => import('@/layouts/DefaultLayout.vue'),
    children: [
      {
        path: '',
        name: 'Archive',
        component: () => import('@/views/archive/index.vue'),
        meta: {
          title: '行为档案',
          icon: 'archive',
          permissions: ['archive:view']
        }
      }
    ]
  },
  {
    path: '/device',
    component: () => import('@/layouts/DefaultLayout.vue'),
    redirect: '/device/box',
    meta: { title: '设备管理', icon: 'device' },
    children: [
      {
        path: 'box',
        name: 'DeviceBox',
        component: () => import('@/views/device/box/index.vue'),
        meta: {
          title: '边缘盒子',
          permissions: ['device:box:view']
        }
      },
      {
        path: 'channel',
        name: 'DeviceChannel',
        component: () => import('@/views/device/channel/index.vue'),
        meta: {
          title: '通道管理',
          permissions: ['device:channel:view']
        }
      }
    ]
  },
  {
    path: '/system',
    component: () => import('@/layouts/DefaultLayout.vue'),
    redirect: '/system/user',
    meta: { title: '系统管理', icon: 'system' },
    children: [
      {
        path: 'user',
        name: 'SystemUser',
        component: () => import('@/views/system/user/index.vue'),
        meta: {
          title: '用户管理',
          permissions: ['system:user:view']
        }
      },
      {
        path: 'role',
        name: 'SystemRole',
        component: () => import('@/views/system/role/index.vue'),
        meta: {
          title: '角色管理',
          permissions: ['system:role:view']
        }
      },
      {
        path: 'config',
        name: 'SystemConfig',
        component: () => import('@/views/system/config/index.vue'),
        meta: {
          title: '系统配置',
          permissions: ['system:config:view']
        }
      },
      {
        path: 'log',
        name: 'SystemLog',
        component: () => import('@/views/system/log/index.vue'),
        meta: {
          title: '日志审计',
          permissions: ['system:log:view']
        }
      }
    ]
  }
]
```

#### 3.3.2 路由守卫

```typescript
// router/guards.ts
import type { Router } from 'vue-router'
import { useUserStore } from '@/stores/modules/user'
import { usePermissionStore } from '@/stores/modules/permission'
import { ElMessage } from 'element-plus'

const whiteList = ['/login', '/404']

export function setupRouterGuards(router: Router) {
  // 前置守卫
  router.beforeEach(async (to, from, next) => {
    const userStore = useUserStore()
    const permissionStore = usePermissionStore()

    // 设置页面标题
    document.title = to.meta.title ? `${to.meta.title} - PDI监测平台` : 'PDI监测平台'

    const hasToken = userStore.isLoggedIn

    if (hasToken) {
      if (to.path === '/login') {
        next('/')
      } else {
        // 检查是否已获取用户信息
        if (!userStore.userInfo) {
          try {
            await userStore.fetchUserInfo()
            // 生成动态路由
            const accessRoutes = await permissionStore.generateRoutes()
            accessRoutes.forEach(route => router.addRoute(route))
            next({ ...to, replace: true })
          } catch (error) {
            await userStore.logout()
            ElMessage.error('获取用户信息失败，请重新登录')
            next(`/login?redirect=${to.path}`)
          }
        } else {
          next()
        }
      }
    } else {
      if (whiteList.includes(to.path)) {
        next()
      } else {
        next(`/login?redirect=${to.path}`)
      }
    }
  })

  // 后置守卫
  router.afterEach(() => {
    // 关闭loading等
  })

  // 错误处理
  router.onError(error => {
    console.error('路由错误:', error)
  })
}
```

### 3.4 HTTP封装

```typescript
// api/request.ts
import axios, {
  type AxiosInstance,
  type AxiosRequestConfig,
  type AxiosResponse,
  type AxiosError
} from 'axios'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useUserStore } from '@/stores/modules/user'

// 创建axios实例
const request: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求队列（用于取消重复请求）
const pendingMap = new Map<string, AbortController>()

// 生成请求key
function getRequestKey(config: AxiosRequestConfig): string {
  return `${config.method}_${config.url}_${JSON.stringify(config.params)}_${JSON.stringify(config.data)}`
}

// 添加请求到队列
function addPending(config: AxiosRequestConfig) {
  const key = getRequestKey(config)
  const controller = new AbortController()
  config.signal = controller.signal
  pendingMap.set(key, controller)
}

// 移除请求
function removePending(config: AxiosRequestConfig) {
  const key = getRequestKey(config)
  if (pendingMap.has(key)) {
    pendingMap.delete(key)
  }
}

// 请求拦截器
request.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    // 添加token
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers = config.headers || {}
      config.headers.Authorization = `Bearer ${userStore.token}`
    }

    // 添加站点ID（数据权限）
    if (userStore.siteId) {
      config.headers = config.headers || {}
      config.headers['X-Site-Id'] = userStore.siteId
    }

    // 防止重复请求
    removePending(config)
    addPending(config)

    return config
  },
  (error: AxiosError) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response: AxiosResponse) => {
    removePending(response.config)

    const { code, message, data } = response.data

    // 成功
    if (code === 200) {
      return data
    }

    // 业务错误
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error: AxiosError) => {
    if (error.config) {
      removePending(error.config)
    }

    const { response } = error

    if (response) {
      switch (response.status) {
        case 401:
          ElMessageBox.confirm('登录已过期，请重新登录', '提示', {
            confirmButtonText: '重新登录',
            cancelButtonText: '取消',
            type: 'warning'
          }).then(() => {
            const userStore = useUserStore()
            userStore.logout()
            window.location.href = '/login'
          })
          break
        case 403:
          ElMessage.error('没有权限执行此操作')
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        case 500:
          ElMessage.error('服务器内部错误')
          break
        default:
          ElMessage.error(response.data?.message || '网络错误')
      }
    } else {
      ElMessage.error('网络连接失败')
    }

    return Promise.reject(error)
  }
)

// 封装请求方法
export function get<T>(url: string, params?: object): Promise<T> {
  return request.get(url, { params }) as Promise<T>
}

export function post<T>(url: string, data?: object): Promise<T> {
  return request.post(url, data) as Promise<T>
}

export function put<T>(url: string, data?: object): Promise<T> {
  return request.put(url, data) as Promise<T>
}

export function del<T>(url: string, params?: object): Promise<T> {
  return request.delete(url, { params }) as Promise<T>
}

export default request
```

### 3.5 权限控制

#### 3.5.1 路由权限

```typescript
// stores/modules/permission.ts
import { defineStore } from 'pinia'
import { asyncRoutes } from '@/router/routes'
import type { RouteRecordRaw } from 'vue-router'
import { useUserStore } from './user'

export const usePermissionStore = defineStore('permission', () => {
  const routes = ref<RouteRecordRaw[]>([])
  const addRoutes = ref<RouteRecordRaw[]>([])

  // 判断是否有权限
  function hasPermission(route: RouteRecordRaw, permissions: string[]): boolean {
    if (route.meta?.permissions) {
      return (route.meta.permissions as string[]).some(p => permissions.includes(p))
    }
    return true
  }

  // 过滤有权限的路由
  function filterAsyncRoutes(
    routes: RouteRecordRaw[],
    permissions: string[]
  ): RouteRecordRaw[] {
    const res: RouteRecordRaw[] = []

    routes.forEach(route => {
      const tmp = { ...route }
      if (hasPermission(tmp, permissions)) {
        if (tmp.children) {
          tmp.children = filterAsyncRoutes(tmp.children, permissions)
        }
        res.push(tmp)
      }
    })

    return res
  }

  // 生成动态路由
  async function generateRoutes(): Promise<RouteRecordRaw[]> {
    const userStore = useUserStore()
    const permissions = userStore.permissions

    const accessedRoutes = filterAsyncRoutes(asyncRoutes, permissions)
    addRoutes.value = accessedRoutes
    routes.value = accessedRoutes

    return accessedRoutes
  }

  return {
    routes,
    addRoutes,
    generateRoutes
  }
})
```

#### 3.5.2 指令权限

```typescript
// directives/permission.ts
import type { Directive, DirectiveBinding } from 'vue'
import { useUserStore } from '@/stores/modules/user'

function checkPermission(el: HTMLElement, binding: DirectiveBinding) {
  const { value } = binding
  const userStore = useUserStore()
  const permissions = userStore.permissions

  if (value && Array.isArray(value)) {
    const hasPermission = value.some(p => permissions.includes(p))
    if (!hasPermission) {
      el.parentNode?.removeChild(el)
    }
  } else {
    throw new Error('v-permission指令需要传入权限数组')
  }
}

export const permission: Directive = {
  mounted(el, binding) {
    checkPermission(el, binding)
  },
  updated(el, binding) {
    checkPermission(el, binding)
  }
}
```

#### 3.5.3 函数权限

```typescript
// composables/useAuth.ts
import { useUserStore } from '@/stores/modules/user'

export function useAuth() {
  const userStore = useUserStore()

  // 检查是否有权限
  function hasPermission(permissions: string | string[]): boolean {
    const perms = Array.isArray(permissions) ? permissions : [permissions]
    return perms.some(p => userStore.permissions.includes(p))
  }

  // 检查是否有角色
  function hasRole(roles: string | string[]): boolean {
    const roleList = Array.isArray(roles) ? roles : [roles]
    return roleList.some(r => userStore.roles.includes(r))
  }

  // 检查是否为超级管理员
  function isSuperAdmin(): boolean {
    return userStore.roles.includes('super_admin')
  }

  // 检查站点权限
  function hasSiteAccess(siteId: string): boolean {
    if (isSuperAdmin()) return true
    return userStore.siteId === siteId
  }

  return {
    hasPermission,
    hasRole,
    isSuperAdmin,
    hasSiteAccess
  }
}
```

---

## 4. 页面组件规划

### 4.1 公共组件清单

| 组件名 | 路径 | 功能描述 |
|--------|------|----------|
| AppLayout | layouts/DefaultLayout.vue | 主布局（侧边栏+头部+内容区） |
| PageContainer | components/templates/PageContainer | 页面容器（面包屑+标题+内容） |
| DataTable | components/molecules/DataTable | 数据表格（分页+排序+筛选） |
| SearchForm | components/molecules/SearchForm | 搜索表单 |
| ImageViewer | components/organisms/ImageViewer | 图片查看器（放大、旋转、下载） |
| StatisticCard | components/molecules/StatisticCard | 统计卡片 |
| StatusTag | components/molecules/StatusTag | 状态标签 |
| AlarmCard | components/organisms/AlarmCard | 报警卡片 |
| DeviceCard | components/organisms/DeviceCard | 设备卡片 |
| DetailDialog | components/templates/DetailDialog | 详情弹窗模板 |

### 4.2 各页面组件拆分

#### 4.2.1 工作台 (Dashboard)

```
views/dashboard/
├── index.vue                 # 页面入口
├── components/
│   ├── StatisticCards.vue    # 统计卡片组（今日报警、设备在线率等）
│   ├── AlarmTrendChart.vue   # 报警趋势图表
│   ├── DeviceStatusChart.vue # 设备状态饼图
│   ├── RecentAlarmList.vue   # 最近报警列表
│   └── QuickActions.vue      # 快捷操作
```

#### 4.2.2 预警中心

```
views/alarm/
├── realtime/
│   ├── index.vue
│   └── components/
│       ├── VideoGrid.vue         # 视频网格布局
│       ├── AlarmNotification.vue # 报警通知组件
│       └── AlarmConfirmModal.vue # 报警确认弹窗
├── history/
│   ├── index.vue
│   └── components/
│       ├── AlarmFilter.vue       # 报警筛选
│       ├── AlarmTable.vue        # 报警表格
│       └── ExportButton.vue      # 导出按钮
└── components/
    ├── AlarmDetailDialog.vue     # 报警详情弹窗（共用）
    └── ImageGallery.vue          # 图片画廊
```

#### 4.2.3 行为档案

```
views/archive/
├── index.vue
├── components/
│   ├── ArchiveTable.vue        # 档案表格
│   ├── ArchiveFilter.vue       # 档案筛选
│   └── ArchiveExport.vue       # 档案导出
└── detail/
    ├── ArchiveDetailDialog.vue # 档案详情弹窗
    └── TimelineView.vue        # 时间线视图
```

#### 4.2.4 设备管理

```
views/device/
├── box/
│   ├── index.vue
│   └── components/
│       ├── BoxTable.vue        # 盒子列表
│       ├── BoxFormDialog.vue   # 盒子表单弹窗
│       └── BoxStatusBadge.vue  # 盒子状态徽章
├── channel/
│   ├── index.vue
│   └── components/
│       ├── ChannelTable.vue    # 通道列表
│       ├── ChannelConfig.vue   # 通道配置
│       └── VideoPreview.vue    # 视频预览
└── components/
    └── DeviceTree.vue          # 设备树形结构
```

#### 4.2.5 系统管理

```
views/system/
├── user/
│   ├── index.vue
│   └── components/
│       ├── UserTable.vue
│       ├── UserFormDialog.vue
│       └── ResetPasswordDialog.vue
├── role/
│   ├── index.vue
│   └── components/
│       ├── RoleTable.vue
│       ├── RoleFormDialog.vue
│       └── PermissionTree.vue    # 权限树
├── config/
│   ├── index.vue
│   └── components/
│       ├── ConfigForm.vue
│       └── SystemInfo.vue
└── log/
    ├── index.vue
    └── components/
        ├── LogTable.vue
        └── LogFilter.vue
```

### 4.3 弹窗组件详细设计

#### 4.3.1 AlarmDetailDialog

```vue
<template>
  <el-dialog
    v-model="visible"
    title="预警详情"
    width="900px"
    class="alarm-detail-dialog"
    destroy-on-close
  >
    <div class="alarm-detail">
      <!-- 左侧图片 -->
      <div class="alarm-detail__image-section">
        <ImageViewer
          :src="alarmData.imageUrl"
          :alt="alarmData.alarmType"
          @zoom="handleZoom"
        />
        <div class="alarm-detail__thumbnails">
          <img
            v-for="(img, index) in alarmData.images"
            :key="index"
            :src="img"
            :class="{ active: currentImage === img }"
            @click="currentImage = img"
          />
        </div>
      </div>

      <!-- 右侧信息 -->
      <div class="alarm-detail__info-section">
        <el-descriptions :column="1" border>
          <el-descriptions-item label="预警类型">
            <StatusTag :type="alarmData.level" :text="alarmData.typeText" />
          </el-descriptions-item>
          <el-descriptions-item label="预警时间">
            {{ formatDateTime(alarmData.createTime) }}
          </el-descriptions-item>
          <el-descriptions-item label="设备名称">
            {{ alarmData.deviceName }}
          </el-descriptions-item>
          <el-descriptions-item label="通道位置">
            {{ alarmData.channelLocation }}
          </el-descriptions-item>
          <el-descriptions-item label="检测区域">
            {{ alarmData.detectArea }}
          </el-descriptions-item>
          <el-descriptions-item label="置信度">
            <el-progress :percentage="alarmData.confidence" />
          </el-descriptions-item>
          <el-descriptions-item label="处理状态">
            <el-tag :type="alarmData.isConfirmed ? 'success' : 'warning'">
              {{ alarmData.isConfirmed ? '已确认' : '待确认' }}
            </el-tag>
          </el-descriptions-item>
        </el-descriptions>

        <div class="alarm-detail__actions">
          <el-button
            v-if="!alarmData.isConfirmed"
            type="primary"
            @click="handleConfirm"
          >
            确认报警
          </el-button>
          <el-button @click="handleViewVideo">查看录像</el-button>
          <el-button @click="handleExport">导出报告</el-button>
        </div>
      </div>
    </div>
  </el-dialog>
</template>
```

---

## 5. 暗黑主题实现

### 5.1 ElementPlus暗黑主题配置

```typescript
// main.ts
import { createApp } from 'vue'
import App from './App.vue'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import 'element-plus/theme-chalk/dark/css-vars.css'

const app = createApp(App)
app.use(ElementPlus)
app.mount('#app')
```

### 5.2 CSS变量定义

```scss
// styles/dark-theme.scss
:root {
  // 基础颜色
  --pdi-bg-primary: #0f172a;
  --pdi-bg-secondary: #1e293b;
  --pdi-bg-tertiary: #334155;

  // 导航栏
  --pdi-nav-bg: #1a1a2e;
  --pdi-nav-text: #94a3b8;
  --pdi-nav-active: #3b82f6;

  // 主色调
  --pdi-primary: #3b82f6;
  --pdi-primary-light: #60a5fa;
  --pdi-primary-dark: #2563eb;

  // 功能色
  --pdi-success: #22c55e;
  --pdi-warning: #f97316;
  --pdi-danger: #ef4444;
  --pdi-info: #3b82f6;

  // 文字颜色
  --pdi-text-primary: #f8fafc;
  --pdi-text-secondary: #e2e8f0;
  --pdi-text-tertiary: #94a3b8;
  --pdi-text-muted: #64748b;

  // 边框
  --pdi-border: #334155;
  --pdi-border-light: #475569;

  // 阴影
  --pdi-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
  --pdi-shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.4);
}

// 覆盖ElementPlus变量
html.dark {
  --el-bg-color: var(--pdi-bg-primary);
  --el-bg-color-page: var(--pdi-bg-primary);
  --el-bg-color-overlay: var(--pdi-bg-secondary);

  --el-text-color-primary: var(--pdi-text-primary);
  --el-text-color-regular: var(--pdi-text-secondary);
  --el-text-color-secondary: var(--pdi-text-tertiary);
  --el-text-color-placeholder: var(--pdi-text-muted);

  --el-border-color: var(--pdi-border);
  --el-border-color-light: var(--pdi-border-light);
  --el-border-color-darker: var(--pdi-border);

  --el-fill-color: var(--pdi-bg-secondary);
  --el-fill-color-light: var(--pdi-bg-tertiary);
  --el-fill-color-blank: var(--pdi-bg-secondary);

  --el-color-primary: var(--pdi-primary);
  --el-color-success: var(--pdi-success);
  --el-color-warning: var(--pdi-warning);
  --el-color-danger: var(--pdi-danger);
}
```

### 5.3 图表适配方案

```typescript
// utils/echarts-theme.ts
export const darkTheme = {
  backgroundColor: 'transparent',
  textStyle: {
    color: '#e2e8f0'
  },
  title: {
    textStyle: {
      color: '#f8fafc'
    }
  },
  legend: {
    textStyle: {
      color: '#94a3b8'
    }
  },
  tooltip: {
    backgroundColor: '#1e293b',
    borderColor: '#334155',
    textStyle: {
      color: '#e2e8f0'
    }
  },
  xAxis: {
    axisLine: {
      lineStyle: {
        color: '#334155'
      }
    },
    axisLabel: {
      color: '#94a3b8'
    },
    splitLine: {
      lineStyle: {
        color: '#1e293b'
      }
    }
  },
  yAxis: {
    axisLine: {
      lineStyle: {
        color: '#334155'
      }
    },
    axisLabel: {
      color: '#94a3b8'
    },
    splitLine: {
      lineStyle: {
        color: '#1e293b'
      }
    }
  }
}
```

### 5.4 图片适配方案

```scss
// 图片容器添加暗色遮罩，避免过亮
.image-container {
  position: relative;

  &::after {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(15, 23, 42, 0.1);
    pointer-events: none;
  }

  img {
    filter: brightness(0.95);
  }
}

// 报警图片高亮
.alarm-image {
  border: 2px solid var(--pdi-danger);
  box-shadow: 0 0 20px rgba(239, 68, 68, 0.3);
}
```

---

## 6. 实时通信

### 6.1 WebSocket客户端封装

```typescript
// composables/useWebSocket.ts
import { ref, onMounted, onUnmounted } from 'vue'
import { useAlarmStore } from '@/stores/modules/alarm'

interface WebSocketOptions {
  url: string
  reconnectInterval?: number
  heartbeatInterval?: number
  maxReconnectAttempts?: number
}

export function useWebSocket(options: WebSocketOptions) {
  const {
    url,
    reconnectInterval = 3000,
    heartbeatInterval = 30000,
    maxReconnectAttempts = 5
  } = options

  const ws = ref<WebSocket | null>(null)
  const isConnected = ref(false)
  const reconnectAttempts = ref(0)
  const heartbeatTimer = ref<number | null>(null)
  const reconnectTimer = ref<number | null>(null)

  const alarmStore = useAlarmStore()

  function connect() {
    if (ws.value?.readyState === WebSocket.OPEN) return

    const token = localStorage.getItem('token')
    const wsUrl = `${url}?token=${token}`

    ws.value = new WebSocket(wsUrl)

    ws.value.onopen = () => {
      console.log('WebSocket连接成功')
      isConnected.value = true
      reconnectAttempts.value = 0
      startHeartbeat()
    }

    ws.value.onmessage = (event) => {
      try {
        const data = JSON.parse(event.data)
        handleMessage(data)
      } catch (error) {
        console.error('解析消息失败:', error)
      }
    }

    ws.value.onclose = () => {
      console.log('WebSocket连接关闭')
      isConnected.value = false
      stopHeartbeat()
      attemptReconnect()
    }

    ws.value.onerror = (error) => {
      console.error('WebSocket错误:', error)
      isConnected.value = false
    }
  }

  function handleMessage(data: any) {
    switch (data.type) {
      case 'alarm':
        // 新报警推送
        alarmStore.addRealtimeAlarm(data.payload)
        if (data.payload.level === 1) {
          alarmStore.showAlarmDialog(data.payload)
        }
        break
      case 'device_status':
        // 设备状态变更
        // deviceStore.updateDeviceStatus(data.payload.deviceId, data.payload.status)
        break
      case 'pong':
        // 心跳响应
        break
      default:
        console.log('未知消息类型:', data.type)
    }
  }

  function send(message: any) {
    if (ws.value?.readyState === WebSocket.OPEN) {
      ws.value.send(JSON.stringify(message))
    }
  }

  function startHeartbeat() {
    heartbeatTimer.value = window.setInterval(() => {
      send({ type: 'ping' })
    }, heartbeatInterval)
  }

  function stopHeartbeat() {
    if (heartbeatTimer.value) {
      clearInterval(heartbeatTimer.value)
      heartbeatTimer.value = null
    }
  }

  function attemptReconnect() {
    if (reconnectAttempts.value >= maxReconnectAttempts) {
      console.error('WebSocket重连次数已达上限')
      return
    }

    reconnectAttempts.value++
    console.log(`WebSocket第${reconnectAttempts.value}次重连...`)

    reconnectTimer.value = window.setTimeout(() => {
      connect()
    }, reconnectInterval)
  }

  function disconnect() {
    stopHeartbeat()
    if (reconnectTimer.value) {
      clearTimeout(reconnectTimer.value)
    }
    ws.value?.close()
    ws.value = null
  }

  onMounted(() => {
    connect()
  })

  onUnmounted(() => {
    disconnect()
  })

  return {
    isConnected,
    connect,
    disconnect,
    send
  }
}
```

### 6.2 实时预警页面使用

```vue
<script setup lang="ts">
import { useWebSocket } from '@/composables/useWebSocket'

const { isConnected } = useWebSocket({
  url: import.meta.env.VITE_WS_URL || 'wss://api.example.com/ws'
})
</script>

<template>
  <div class="realtime-alarm">
    <div class="connection-status" :class="{ connected: isConnected }">
      {{ isConnected ? '实时连接中' : '连接断开' }}
    </div>
    <!-- 预警内容 -->
  </div>
</template>
```

---

## 7. 开发规范

### 7.1 命名规范

```typescript
// 文件命名
// 组件: PascalCase (AppHeader.vue, AlarmDetailDialog.vue)
// 工具函数: camelCase (formatDate.ts, useAuth.ts)
// 常量: SNAKE_CASE (constants.ts)

// 变量命名
// 布尔值: is/has/can/should + 名词 (isLoading, hasPermission)
// 数组: 名词复数 (users, alarmList)
// 函数: 动词 + 名词 (getUserInfo, handleSubmit)

// Vue组件命名
// 模板ref: 组件名 + Ref (tableRef, formRef)
// 事件处理: handle + Event (handleClick, handleSubmit)
// 计算属性: 名词或形容词 (fullName, isValid)

// CSS类名 (BEM命名)
// Block: .alarm-card
// Element: .alarm-card__header, .alarm-card__image
// Modifier: .alarm-card--urgent, .alarm-card__button--disabled
```

### 7.2 Git提交规范

```bash
# 提交格式: <type>(<scope>): <subject>
# 示例: feat(alarm): 添加实时预警声光提示功能

# Type类型
feat:     新功能
fix:      修复bug
docs:     文档更新
style:    代码格式（不影响功能）
refactor: 重构
test:     测试相关
chore:    构建/工具相关
perf:     性能优化

# Scope范围
dashboard: 工作台
alarm:     预警中心
archive:   行为档案
device:    设备管理
system:    系统管理
common:    公共组件
api:       接口相关

# 示例
feat(alarm): 添加报警详情弹窗图片查看功能
fix(device): 修复设备状态显示不正确的问题
docs: 更新README.md安装说明
style: 统一代码缩进格式
```

### 7.3 代码审查清单

```markdown
## 代码审查检查项

### 功能完整性
- [ ] 功能实现符合需求文档
- [ ] 边界条件处理（空数据、loading、错误状态）
- [ ] 权限控制正确实现

### 代码质量
- [ ] 命名规范符合项目约定
- [ ] 无console.log调试代码
- [ ] 无未使用的变量/导入
- [ ] 类型定义完整（无any滥用）

### Vue最佳实践
- [ ] 组件name属性已设置
- [ ] Props类型定义完整
- [ ] Emits事件声明完整
- [ ] 响应式数据使用正确（ref/reactive）
- [ ] 生命周期钩子使用正确

### 性能优化
- [ ] 大数据列表使用虚拟滚动
- [ ] 图片懒加载
- [ ] 组件懒加载
- [ ] 避免不必要的重渲染

### 安全性
- [ ] XSS防护（v-html使用安全）
- [ ] 敏感信息未硬编码
- [ ] 权限校验完整

### 可访问性
- [ ] 图片有alt属性
- [ ] 表单有关联label
- [ ] 颜色对比度符合标准
```

---

## 8. 环境配置

### 8.1 环境变量

```bash
# .env.development
VITE_APP_TITLE=PDI智能监测平台
VITE_API_BASE_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws
VITE_UPLOAD_URL=http://localhost:8080/upload
```

```bash
# .env.production
VITE_APP_TITLE=PDI智能监测平台
VITE_API_BASE_URL=/api
VITE_WS_URL=wss://pdi.example.com/ws
VITE_UPLOAD_URL=/upload
```

### 8.2 Vite配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
      imports: ['vue', 'vue-router', 'pinia'],
      dts: 'src/types/auto-imports.d.ts'
    }),
    Components({
      resolvers: [ElementPlusResolver()],
      dts: 'src/types/components.d.ts'
    })
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  },
  build: {
    target: 'es2015',
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'echarts': ['echarts'],
          'vendor': ['vue', 'vue-router', 'pinia', 'axios']
        }
      }
    }
  }
})
```

---

## 9. 附录

### 9.1 目录创建脚本

```bash
#!/bin/bash
# init-project.sh

mkdir -p src/{api/modules,assets/{images,icons},components/{atoms,molecules,organisms,templates},composables,directives,layouts,router,stores/{modules,plugins},styles,types,utils,views/{login,dashboard,alarm/{realtime,history},archive,device/{box,channel},system/{user,role,config,log}}}

echo "项目目录结构创建完成"
```

### 9.2 依赖安装清单

```bash
# 核心依赖
npm install vue@^3.4.0 vue-router@^4.2.0 pinia@^2.1.0
npm install element-plus@^2.5.0 @element-plus/icons-vue
npm install axios@^1.6.0

# 开发依赖
npm install -D typescript vue-tsc
npm install -D vite @vitejs/plugin-vue
npm install -D sass
npm install -D eslint prettier @typescript-eslint/*
npm install -D unplugin-auto-import unplugin-vue-components
```

---

**文档版本**: v1.0
**最后更新**: 2024年
**编写人**: 前端架构师
